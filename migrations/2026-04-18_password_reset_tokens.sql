-- password_reset_tokens is created in 2026-02-08_orgs_users_auth.sql (token_hash, expires_at, used_at).
-- The application continues to store SHA-256 hashes of reset tokens for /reset-password validation.
-- Intentional no-op placeholder so migration ordering matches the handoff checklist without altering the live table.

SELECT 1;
