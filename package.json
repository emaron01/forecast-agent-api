{
  "name": "forecast-agent-endpoint",
  "version": "1.0.0",
  "description": "Matthew Voice AI",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build",
    "start:web": "npm --prefix web run start",
    "postinstall": "npm --prefix web install",
    "dev": "npm run dev:web",
    "build": "npm run build:web",
    "start": "npm run start:web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.17.2",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
